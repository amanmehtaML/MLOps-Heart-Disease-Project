name: Heart Disease MLOps CI Pipeline

on:
  # Jab bhi code push ya Pull Request 'main' branch mein aaye, tab run karo
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: ⬇️ Checkout code
      uses: actions/checkout@v4

    - name: 🐍 Set up Python 3.10
      uses: actions/setup-python@v4
      with:
        # Python version jo aapne requirements.txt ke liye set kiya tha
        python-version: '3.10'

    - name: 📦 Install dependencies
      run: |
        python -m pip install --upgrade pip
        # Ab dependencies install ho jaayengi (scikit-learn version fix ho chuka hai)
        pip install -r requirements.txt
        # Linting tool install karo
        pip install flake8 

    # 1. Linting Step (Code Style Check)
    - name: 🧹 Run Linting (Flake8)
      run: |
        # src/ folder ke code ko check karega
        flake8 src/ --count --show-source --statistics

    # 2. Unit Testing Step (Automated Testing)
    - name: 🧪 Run Pytest Unit Tests
      run: |
        # FIX: Pytest executable ko Python module ke through chalao
        python -m pytest test/ 

    # 3. Model Training and MLflow Logging Step (Reproducibility & Artifacts)
    - name: 🚂 Train Model and Log to MLflow
      # Yeh step tabhi chalega jab data/cleveland.data GitHub par present ho
      run: |
        python src/train_model.py
        
    - name: 📄 Upload MLflow Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: mlflow-artifacts
        path: mlruns/
        retention-days: 7